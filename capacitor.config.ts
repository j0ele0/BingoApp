import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hsos.ellerbracke.kramer',
  appName: 'BingoApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
